VITE_API_PORT=8000
VITE_API_HOST=localhost
# 控制台 /ws 实时推送（本仓库 FastAPI 默认未实现该路由；不配则不会连，避免控制台报错）
# VITE_CONSOLE_WS_URL=ws://localhost:3000/ws
# 可选：覆盖默认的 /nanobot-ws；设为空可禁用 nanobot 频道聊天
# VITE_NANOBOT_WS_BASE=/nanobot-ws
